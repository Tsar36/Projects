# Example 5: Running a Rails Migration
# If you are using Ruby on Rails, you might need to run Rails’ migration command:

#!/bin/sh

# Navigate to the Rails project directory (adjust as needed)
cd /path/to/rails/project

# Apply Rails migrations
bundle exec rake db:migrate
